name: Snyk SCA Scan

on:
  workflow_dispatch:   # 👈 allows manual runs from Actions tab
  push:
    paths:
      - 'vulcode/**'
  pull_request:
    paths:
      - 'vulcode/**'

jobs:
  snyk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ✅ Ensure pip pulls prebuilt wheels instead of compiling PyYAML
      - name: Upgrade pip and wheel
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install --only-binary=:all: PyYAML==6.0.1

      - name: Install dependencies
        run: |
          cd vulcode
          pip install -r requirements.txt

      - name: Run Snyk SCA
        uses: snyk/actions/python@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --file=vulcode/requirements.txt --json-file-output=report.json

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: snyk-sca-report
          path: report.json

      # 👀 Optional: show results directly in logs
      - name: Show Snyk results
        run: |
          cat report.json | jq '.vulnerabilities[] | {id, severity, title}'
