name: Snyk SCA Scan

on:
  workflow_dispatch:   # Manual trigger from Actions tab
  push:
    paths:
      - 'vulcode/**'
  pull_request:
    paths:
      - 'vulcode/**'

jobs:
  snyk:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout your code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Install jq for JSON parsing
      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      # Step 3: Install Python build dependencies (for packages like PyYAML)
      - name: Install Python build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3-dev libffi-dev libssl-dev

      # Step 4: Upgrade pip and install Python dependencies
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r vulcode/requirements.txt

      # Step 5: Run local Snyk test (outputs JSON file)
      - name: Run Snyk SCA (local test)
        uses: snyk/actions/python@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: --file=vulcode/requirements.txt --package-manager=pip --skip-unresolved --json-file-output=report.json

      # Step 6: Push results to Snyk dashboard
      - name: Push results to Snyk dashboard
        uses: snyk/actions/python@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: monitor
          args: --file=vulcode/requirements.txt --package-manager=pip

      # Step 7: Convert JSON to HTML
      - name: Convert JSON to HTML
        run: python convert_to_html.py

      # Step 8: Upload both JSON and HTML reports
      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: snyk-sca-reports
          path: |
            report.json
            report.html

      # Step 9: Show CLI summary in logs
      - name: Show Snyk results (CLI summary)
        run: |
          cat report.json | jq '.vulnerabilities[] | {id, severity, title}'
